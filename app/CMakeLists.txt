INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)

ADD_EXECUTABLE(CloseApp
     ${PROJECT_SOURCE_DIR}/app/main.cpp
)
TARGET_LINK_LIBRARIES(CloseApp
    ${PROJECT_NAME}
    ${CONFIGURATIONS}
    ${CMAKE_DL_LIBS}
    ${CONAN_LIBS}
)

